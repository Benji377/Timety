import 'package:flutter/material.dart';

// Internal data class for wrap-up image generation
class WrapUpStatRow {
  final IconData icon;
  final Color color;
  final String value;
  final String label;
  const WrapUpStatRow(this.icon, this.color, this.value, this.label);
}
